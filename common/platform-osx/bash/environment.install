#!/bin/bash

# Link safe (ish) GNU coreutils into a
GNU_OVERRIDE_DIR="$HOME/.local/lib/gnu-override/bin"

mkdir -p "$GNU_OVERRIDE_DIR"
cd  "$GNU_OVERRIDE_DIR"

ln -fs "${NIX_USER_PROFILE_DIR}/profile/bin/gls" ls
ln -fs "${NIX_USER_PROFILE_DIR}/profile/bin/gdircolors" dircolors
